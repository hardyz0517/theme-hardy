# Hardy Plugin Support Matrix

Status: core/plugin contracts documented; optional content plugin audit is partially complete on Halo 2.25.4 (2026-08-06)

Source revision: Halo documentation commit [`b466520ff9f8cfbbfa2ea6547d2154f5f24bd94b`](https://github.com/halo-dev/docs/tree/b466520ff9f8cfbbfa2ea6547d2154f5f24bd94b), retrieved on 2026-08-06.

This file records plugin contracts that Hardy consumes directly. Public HTML from the reference site is not sufficient evidence for plugin APIs.

| Capability     | Plugin identifier                     | Version observed on `https://hardyzheng.com` | Hardy behavior                                                                                                                                                                                                                                                                                                   | Runtime states still required                                                                                                                                                                      |
| -------------- | ------------------------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Search Widget  | `PluginSearchWidget`                  | 1.7.1                                        | Render search triggers only when `pluginFinder.available('PluginSearchWidget')` is true. The browser adapter calls `window.SearchWidget.open()` if present.                                                                                                                                                      | Desktop/mobile trigger, open, no-results, Escape close, and dark-mode inheritance verified; absent/disabled and long-result matrix remains pending.                                                |
| Comments       | Comment Widget through `halo:comment` | 3.1.2                                        | Render comments only behind `haloCommentEnabled`. Posts use `group="content.halo.run"` and `kind="Post"`; single pages use `kind="SinglePage"`.                                                                                                                                                                  | SinglePage/Post identities, reversible disabled state, 8-comment/3-reply populated thread, and 390px auto/light/dark rendering verified. A true uninstalled-plugin page fixture remains unclaimed. |
| Shiki          | `shiki`                               | 1.4.2                                        | Leave the plugin-owned code custom element and assets intact; Hardy scopes prose styles around it.                                                                                                                                                                                                               | A published code fixture produces `<pre>` output; long-code, absent-plugin, and both color modes remain pending.                                                                                   |
| Hyperlink card | `editor-hyperlink-card`               | 1.9.1                                        | Leave the plugin-owned custom element and assets intact; no private DOM selectors are required.                                                                                                                                                                                                                  | Populated card, load error, mobile width, absent plugin, and both color modes remain pending.                                                                                                      |
| KaTeX          | `plugin-katex`                        | 3.0.0                                        | Leave plugin-owned math markup and stylesheet intact; prose styles must not override its internals.                                                                                                                                                                                                              | A published fixture contains `.katex-block` and MathML output; non-empty formula, absent-plugin, and both color modes remain pending.                                                              |
| Color scheme   | Official plugin color-scheme contract | Halo 2.25.4 public response                  | Set `data-color-scheme` on `<html>` to `auto`, `light`, or `dark`.                                                                                                                                                                                                                                               | Verified `auto` rendering and mobile control presence; server-side forced modes need console UI verification.                                                                                      |
| Moments        | `PluginMoments`                       | 1.16.1                                       | Optional `/moments` and `/moments/{name}` templates, guarded by `pluginFinder.available('PluginMoments')`; list/detail use the documented moment content, tags, stats, pagination, and comment identity.                                                                                                         | Source templates are present. Installed/populated browser evidence, media variants, absent-plugin behavior, and full state matrix remain pending.                                                  |
| Links          | `PluginLinks`                         | v2.3.0-beta.4                                | Optional `/links` template with grouped/taxonomy-filtered two/four-column cards, 48px logo fallback, external-link semantics, and official `halo:comment` identity. Visitor applications are intentionally not emitted by Hardy in this phase. See [`optional-content-plugins.md`](optional-content-plugins.md). | Source contract verified; empty state, grouped populated fixture, broken external URL, and 390px layout verified. Disabled/absent and full visual states remain pending.                           |
| Photos         | `PluginPhotos`                        | v2.1.2                                       | Optional `/photos` and `/photos/{name}` templates, guarded by `pluginFinder.available('PluginPhotos')`; list/detail consume documented media, groups, neighbors, and Photo comment identity. See [`optional-content-plugins.md`](optional-content-plugins.md).                                                   | Source contract and empty state verified after installation. Populated media/detail, disabled/absent, and mobile visual states remain pending.                                                     |

The full M7 gate, fixture requirements, and reproducible probes are in [`../testing/m7-plugin-audit.md`](../testing/m7-plugin-audit.md). No optional route is complete until its source contract and runtime states are verified.
